import React from 'react';
import './errorMessage.scss';

const ErrorMessage = () => {   
    return (
        <div className="errorMessage">Something goes wrong. Try again.</div>
    )
}

export default ErrorMessage;